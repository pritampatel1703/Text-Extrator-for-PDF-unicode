FROM docker.elastic.co/elasticsearch/elasticsearch:8.13.4

# We expose 9200 for REST API access
EXPOSE 9200
EXPOSE 9300

# The data directory will be mounted by Render as a persistent disk
# at /usr/share/elasticsearch/data

# Environment variables are provided by render.yaml
